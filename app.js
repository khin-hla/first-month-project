const reactNav = document.querySelector('.react-nav');
const burger = document.querySelector('.bars')

burger.addEventListener('click', () => {
  reactNav.classList.toggle('open')
})

const navBtn=document.querySelector('.start-btn')
const login=document.querySelector('.login-link')
const navLogin=document.querySelector('.nav-login')
const navButton=document.querySelector('.nav-button')


navBtn.addEventListener('click', ()=> {
  alert("Sorry! We don't provide this version yet.")
})

login.addEventListener('click', ()=> {
  alert("Sorry! We don't provide this version yet.")
})

navLogin.addEventListener('click', ()=> {
  alert("Sorry! We don't provide this version yet.")
})

navButton.addEventListener('click', ()=> {
  alert("Sorry! We don't provide this version yet.")
})